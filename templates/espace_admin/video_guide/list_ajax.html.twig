<table class="table table-hover table-striped verticle-middle dataTable_video_guide" id="dataTable_video_guide" width="99%">
    <thead>
    <tr>
        <td class="encart_home_head" style="width: 25%" >Titre</td>
        <td class="encart_home_head" style="width: 50%" >Lien</td>
        <td class=""></td>
    </tr>
    </thead>
    <tbody>
    {% for row in partners|reverse %}
        <tr id="testimonial_{{ row.id }}">
            <td><span class="encart_home_body">{{ row.titre }}</span></td>
            <td><span class="encart_home_body">{{ row.url }}</span></td>
            <td>
                <div class="actions">
                    <a href="{{ path('app_espace_admin_video_guide_edit', {'id' : row.id}) }}"
                       role="button"
                       class="btn-icon-right btn btn-danger encart_home_body px-5"
                    >
                        Modifier
                    </a>
                    <a href="#" data-video_guide="{{ row.id }}" class="delete_video_guide">
                        <i class="fa fa-trash text-dark" aria-hidden="true"></i>
                    </a>
                </div>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>