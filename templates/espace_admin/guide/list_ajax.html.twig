{#
<table class="table table-hover table-striped verticle-middle dataTable_guide" id="dataTable_guide">
    <thead>
    <tr>
        <td class="encart_home_head ">-</td>
        <td class="encart_home_head text-green">Question</td>
        <td class="encart_home_head text-green">Reponse</td>
        <td class="encart_home_head text-green">Catégorie</td>
        <th></th>
    </tr>
    </thead>
    <tbody>
    {% for row in guides %}
        <tr>
            <td class="encart_home_body ">{{ row.id }} </td>
            <td class="encart_home_body ">{{ row.question }}</td>
            <td class="encart_home_body ">{{ row.response|raw }}</td>
            <td class="encart_home_body ">{{ row.category.title }}</td>
            <td align="center">
                <a href="" data-guide="{{ row.id }}" class="delete_guide">
                    <i class="fa fa-trash text-danger" aria-hidden="true"></i>
                </a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>#}
<div class="row">
    <div class="col-lg-4 list_meeting_head">
        {{ knp_pagination_sortable(guides, 'Question', 'question') }}
    </div>
    <div class="col-lg-5 list_meeting_head">
        {{ knp_pagination_sortable(guides, 'Réponse', 'response') }}
    </div>
    <div class="col-lg-2 list_meeting_head text-left">
        {{ knp_pagination_sortable(guides, 'Catégorie', 'category.title') }}
    </div>
    <div class="col-lg-1 list_meeting_head"></div>
</div>
<div class="row">
    {% for row in guides|reverse %}
        <div class="col-sm-6 col-lg-12  mt-2">
            <div class="row card-borderer mx-1">
                <div class="col-lg-4 d-flex-center-left">
                    <span>{{ row.question }}</span>
                </div>
                <div class="col-lg-5 d-flex-center-left">
                    {{ row.response|raw }}
                </div>
                <div class="col-lg-2 d-flex-center-left">
                    <span>{{ row.category.title }}</span>
                </div>
                <div class="col-lg-1 d-flex-center-left">
                    <a href="" data-guide="{{ row.id }}" class="delete_guide">
                        <i class="fa fa-trash text-danger" aria-hidden="true"></i>
                    </a>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
<div class="list_pagination">{{ knp_pagination_render(guides) }}</div>