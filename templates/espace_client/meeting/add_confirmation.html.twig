{% extends 'base-espace-client.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row mt-5 text-center">
        <div class="col-lg-12 ">
            <p><i class="fa fa-check-circle text-green fa-3x" aria-hidden="true"></i></p>
            <p>
                Votre réunion à bien été créée<br>
                <span class="text-danger">{{ link }}</span><br>
                Accéder directementà celle-ci ou envoyer ce lien<br>
                à d'autres participants
            </p>
            <p class="my-4">
                <a class="btn  btn-sm login text-center" href="{{ path('app_espace_client_meeting_list') }}">Voir mes réunions</a>
            </p>
        </div>
    </div>
{% endblock %}