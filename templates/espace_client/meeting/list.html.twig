{% extends 'base-espace-client.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="row justify-content-center ">
        <div class="col-9 col-lg-6 text-center ">
            {% include 'shared/flash_alert/index.html.twig' %}
        </div>
    </div>
    <div class="row">
        <div class="col-7">
            <span class="text-green">Mes réunions</span>
        </div>
        <div class="col-5">
            <div class="w-100 text-right">
                {% if user.active %}
                    <a class="btn  btn-large py-2 login" href="{{ path('app_espace_client_meeting_add') }}" role="button">
                        <span class="d-none d-sm-inline-block" style="padding-bottom: 5px">
                            Ajouter une réunion
                        </span>
                    </a>
                {% else %}
                    <span class="btn  btn-large py-2 btn-outline-danger" role="button">
                        Ajout réunion désactivé pour les utilisateurs désactivés.
                    </span>
                {% endif %}
            </div>
        </div>
    </div>


    <div class="mt-5 container-fluid max-2000 pr-3" id="list-meetings">

        <div class="row">
            <div class="col-lg-2 list_meeting_head">
               {{ knp_pagination_sortable(meetings, 'Début', 'date') }}
            </div>
            <div class="col-lg-3 list_meeting_head">
               {{ knp_pagination_sortable(meetings, 'Sujet', 'subject') }}
            </div>
            <div class="col-lg-7 list_meeting_head">
                Identifiant de réunion
            </div>
        </div>
        <div class="row">
            {% for row in meetings|reverse %}
                <div class="col-sm-6 col-lg-12 mt-2">
                    <div class="row card-borderer">
                        <div class="col-lg-2 d-flex-center-left">
                            <span>{{ row.date|date("d/m/yy") }} à  {{ row.date|date("H:i") }}</span>
                        </div>
                        
                        <div class="col-lg-3 d-flex-center-left">
                            {{ row.subject }}
                        </div>

                        <div class="col-lg-3 d-flex-center-left">
                            <input class="input_copy" style="color: #50697C; width: 99%" type="text" id="linkInput_{{ row.id }}" value="{{ baseUrl }}/reunion/{{ row.identifiant }}" readonly>
                        </div>
                        <div class="col-lg-4 actions link_identifiant d-flex-center-right" >
                            <div class="w-100">
                                <div class="pull-right">
                                    <a class="detail_meeting encart_home_body text-green" href="#" data-meeting="{{ row.id }}">
                                        <span class="mx-2"> Info + </span>
                                    </a>
                                    
                                    <button class="list_table background-red btn mr-2 px-5" role="button" type="button" onclick="return copyToPaste({{ row.id }});" {{ row.status ? 'disabled="disabled"' : '' }} data-target="#link-meeting">
                                        <span class="d-none d-lg-inline-block pb-1">Copier le lien</span>
                                        <span class="fa fa-clone ml-2" aria-hidden="true"></span>
                                    </button>
                                    <a href="#" data-meeting="{{ row.id }}"  class="delete_meeting">
                                        <img src="{{ asset('build/images/iboo-Trash.svg') }}">
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="list_pagination">{{ knp_pagination_render(meetings) }}</div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <link href="{{ asset('build/css/toastr.min.css') }}" rel="stylesheet"/>
    <script src="{{ asset('build/js/toastr.min.js') }}"></script>
    <script>
        function copyToPaste(id) {
            var copyText = document.getElementById("linkInput_" + id);
            copyText.select();
            copyText.setSelectionRange(0, 99999);
            document.execCommand("copy");

            toastr.options = {
                "closeButton": true,
                "debug": false,
                "newestOnTop": false,
                "progressBar": true,
                "positionClass": "toast-top-right",
                "preventDuplicates": false,
                "onclick": null,
                "showDuration": "300",
                "hideDuration": "1000",
                "timeOut": "2000",
                "extendedTimeOut": "1000",
                "showEasing": "swing",
                "hideEasing": "linear",
                "showMethod": "fadeIn",
                "hideMethod": "fadeOut"
            };

            toastr["success"]("Lien de la réunion copié.")
        }
    </script>
{% endblock %}