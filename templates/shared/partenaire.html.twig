<h2 class="text-center encart_home_head text-green">Ils nous font confiance </h2>
<div class="rectangle text-center" style="padding: 60px 0px">
    {% for row in partners %}
        <div class="col-md-3 rectangle-logo">
            <a href="{{ row.webSite }}" target="_blank">
                <img  class="img-fluid partenaire-logo" src="{{ asset('uploads/images/' ~ row.pdc) }}">
            </a>
        </div>
    {% endfor %}
</div>
