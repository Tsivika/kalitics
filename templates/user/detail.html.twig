{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
    <div class="jumbotron text-center">
        <h2>{{ title }}</h2>
    </div>
    <div class="m-5">
        <div class="text-right mb-3">
            <a href="{{ path('list_user') }}" class="btn btn-info" role="button" >Liste des utilisateurs</a>
        </div>
        <div class="form-group row">
            <label for="text3" class="col-3 col-form-label font-weight-bold">Matricule: </label>
            <label for="text3" class="col-4 col-form-label">{{ user.registerNumber }}</label>
        </div>
        <div class="form-group row">
            <label for="text1" class="col-3 col-form-label font-weight-bold">Nom</label>
            <label for="text1" class="col-4 col-form-label">{{ user.lastname }}</label>
        </div>
        <div class="form-group row">
            <label for="text2" class="col-3 col-form-label font-weight-bold">Prénom</label>
            <label for="text2" class="col-4 col-form-label">{{ user.firstname }}</label>
        </div>
    </div>

{% endblock %}
