{% extends 'baseEmail.html.twig' %}

{% block content %}
    <p class="encart_home_head"><h1>{{ message0|raw }}</h1></p>
    {% if subject_meeting %}
        <p class="encart_home_head"><h3>{{ subject_meeting|raw }}</h3></p>
    {% endif %}
    {% if description_meeting %}
        <p class="encart_home_head"><h3>{{ description_meeting|raw }}</h3></p>
    {% endif %}

    <p class="encart_home_head"><h2>{{ join_meeting|raw }}</h2></p>
    <a href="{{ urlMeeting }}" style="display:inline-block;background:#00c9ae;color:#FFFFFF;font-family:Helvetica;font-size:14px;font-weight:bold;line-height:120%;margin:0;text-decoration:none;text-transform:none;padding:10px 25px;mso-padding-alt:0px;border-radius:5px;" target="_blank">
        Rejoindre la réunion
    </a>
    <p class="encart_home_head"><h2>{{ pwd_meeting }} {{ pwd|raw }}</h2> </p>
    <p class="encart_home_head"><h2>{{ date_meeting }} {{ date|date('d/m/Y') }}</h2> </p>
    <p class="encart_home_head"><h5>{{ signature|raw }}</h5> </p>

{% endblock %}